<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SiteMap">
    <select id="getSiteInfo" resultType="Map">
        SELECT
            s.id,
            c.`name` as country_name,
            s.`name` as site_name,
            s.lat,
            s.lng,
            s.city,
            s.ac_capacity,
            s.dc_capacity,
            s.status,
            s.updated_date,
            s.created_date
        FROM
            site s
        JOIN country c ON c.id = s.id_country
        WHERE SHA1(s.id) = #{hash_id}
    </select>

    <select id="getDeviceList" resultType="Map">
        SELECT
            d.id,
            d.devicename,
            d.lat,
            d.lng,
            dt.image,
        CASE
            WHEN EXISTS (
                SELECT 1
                FROM alert a
                JOIN error e ON e.id = a.id_error
                WHERE a.id_device = d.id
                AND e.error_code = 1001
                AND a.status = 1
            )
            THEN 0
            ELSE 1
        END AS status
        FROM device d
        JOIN device_type dt ON dt.id = d.id_device_type
        WHERE SHA1(d.id_site) = #{hash_id}
    </select>

    <select id="getDeviceTable" resultType="Map">
        SELECT
            d.id,
            d.devicename,
            d.datatablename
        FROM
            device d
        WHERE d.id = #{id}
    </select>

    <select id="getParamSlugsByDevice" resultType="Map">
        SELECT dp.id, dp.name, dp.standard_name, dp.slug, dp.unit
        FROM device_parameters dp
        JOIN site_map_device_parameter smdp ON smdp.parameter_id = dp.id
        WHERE smdp.device_id = #{id}
    </select>

    <select id="getLatestMetrics" resultType="map">
        SELECT *
        FROM ${datatablename}
        ORDER BY time DESC
        LIMIT 1
    </select>

    <select id="getListParameterByDeviceGroup" resultType="Map">
        SELECT
            dp.*
        FROM device_parameters dp
        LEFT JOIN device_group dg ON dg.id = dp.id_device_group
        WHERE dg.id = #{id_device_group}
    </select>

    <insert id="insertParameterByDevice" parameterType="Map">
        INSERT INTO site_map_device_parameter (device_id, parameter_id)
        VALUES
        <foreach collection="list_parameters" item="paramId" separator=",">
            (#{id}, #{paramId})
        </foreach>
    </insert>

    <delete id="deleteParameterByDevice" parameterType="int">
        DELETE FROM site_map_device_parameter
        WHERE device_id = #{id}
    </delete>

    <select id="getParameterByDevice" resultType="Map">
        SELECT
            smdp.device_id,
            smdp.parameter_id
        FROM site_map_device_parameter smdp
        WHERE smdp.device_id = #{id}
    </select>
</mapper>