<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CustomerView">
	
	<select id="getDevicesBySite" resultType="com.nwm.api.entities.DeviceEntity">
		SELECT
			d.id,
			d.devicename,
			d.datatablename,
			d.id_site,
			d.is_excluded_meter,
			d.reverse_poa,
			d.is_weather_to_calculate_expected,
			dt.id AS id_device_type,
			t.value AS timezone_value
		FROM
			device d
			JOIN device_type dt ON d.id_device_type = dt.id 
			JOIN site s ON s.id = d.id_site
			JOIN time_zone t ON t.id = s.id_time_zone
		WHERE
			IF(#{hash_id} IS NOT NULL, SHA1(s.id) = #{hash_id}, d.id_site = #{id_site})
			AND d.`status` = 1 
			AND d.is_delete = 0
			AND (CONVERT_TZ(NOW(), 'UTC', t.value) <![CDATA[ < ]]> s.expiration OR s.expiration IS NULL)
			AND (d.meter_type = 3 OR d.meter_type IS NULL)
			<include refid="com.nwm.common.deviceByDomainCondition"/>
	</select>
	
	<select id="getSelectedPOABySite" resultType="com.nwm.api.entities.ExpectedBySiteDTO">
		SELECT
			ids_device_poa,
			ids_device_rpoa,
			ids_device_panel_temp,
			ids_device_ambient_temp
		FROM
			site
		WHERE
			id = #{id_site}
	</select>
	
	<select id="getHiddenDataListByDevice" resultType="Map">
		SELECT
			date_from,
			date_to
		FROM
			hidden_data
		WHERE
			id_device = #{id}
			AND status = 1
			AND is_delete = 0
	</select>
	
	<select id="getHiddenDataListBySite" resultType="Map">
		SELECT
			hd.id_device,
			hd.date_from,
			hd.date_to
		FROM
			hidden_data hd
			LEFT JOIN device d ON d.id = hd.id_device
			LEFT JOIN site s ON s.id = d.id_site
		WHERE
			s.id = #{id_site}
			AND s.status = 1
			AND s.is_delete = 0
			AND d.status = 1
			AND d.is_delete = 0
			AND hd.status = 1
			AND hd.is_delete = 0
	</select>
	
	<select id="getDataEnergy" resultType="com.nwm.api.entities.ClientMonthlyDateEntity" >
			SELECT
				t.id,
				t.categories_time,
				t.time_full,
				IF(
					(dpf.min_value IS NOT NULL AND t.chart_energy_kwh <![CDATA[ < ]]> dpf.min_value) OR (dpf.max_value IS NOT NULL AND t.chart_energy_kwh <![CDATA[ > ]]> dpf.max_value),
					NULL,
					t.chart_energy_kwh
				) AS chart_energy_kwh
			FROM
				(
					SELECT
						d.id,
						dv.time,
						<include refid="com.nwm.common.timeGroupBy">
							<property name="time" value="CONVERT_TZ( dv.time, 'UTC', t.value )" />
							<property name="interval" value="data_send_time" />
						</include> AS time_full,
						<include refid="SitesAnalytics.categoriesTime"><property name="time" value="CONVERT_TZ( dv.time, 'UTC', t.value )" /></include> AS categories_time,
						CASE
							WHEN DATEDIFF(#{end_date}, #{start_date}) <![CDATA[ < ]]> 5 THEN
								m.nvmActivePower
							ELSE
								<include refid="com.nwm.common.intervalEnergyCalculation">
									<property name="datetime" value="CONVERT_TZ( dv.time, 'UTC', t.value )" />
									<property name="interval" value="data_send_time" />
									<property name="table" value="${datatablename}" />
									<property name="id" value="#{id}" />
									<property name="accumulatedEnergySlug" value="dv.nvmActiveEnergy" />
								</include>
						END AS chart_energy_kwh
					FROM
						${datatablename} dv
						JOIN device d ON d.id = dv.id_device
						JOIN site s ON s.id = d.id_site
						JOIN time_zone t ON t.id = s.id_time_zone
						JOIN (
							SELECT
								(
									<include refid="com.nwm.common.leadTimeValidCondition">
										<property name="time" value="CONVERT_TZ(dv.time, 'UTC', t.value)" />
										<property name="interval" value="data_send_time" />
									</include>
								) AS leadTimeValid,
								MIN(dv.time) AS minTime,
								MAX(dv.time) AS maxTime,
								AVG(dv.nvmActivePower) AS nvmActivePower
							FROM
								${datatablename} dv
								JOIN device d ON d.id = dv.id_device
								JOIN site s ON s.id = d.id_site
								JOIN time_zone t ON t.id = s.id_time_zone
							WHERE
								s.id = #{id_site}
								AND s.`status` = 1 
								AND d.id = #{id}
								AND d.`status` = 1
								AND dv.time BETWEEN CONVERT_TZ(#{start_date}, t.value, 'UTC') AND CONVERT_TZ(#{end_date}, t.value, 'UTC')
								<foreach item="hidden" index="index" collection="hidden_data_list" open=" AND " separator=" AND ">
									dv.time NOT BETWEEN CONVERT_TZ(#{hidden.date_from}, t.value, 'UTC') AND CONVERT_TZ(#{hidden.date_to}, t.value, 'UTC')
							    </foreach>
							GROUP BY
								<include refid="com.nwm.common.timeGroupBy">
									<property name="time" value="CONVERT_TZ( dv.time, 'UTC', t.value )" />
									<property name="interval" value="data_send_time" />
								</include>
						) m ON m.minTime = dv.time AND dv.id_device = #{id}
				) t
				JOIN device d ON d.id = t.id
				LEFT JOIN device_parameters dp ON dp.id_device_group = d.id_device_group AND dp.is_user_defined = 1 AND dp.is_energy = 1 AND ${filterEnabled} = true
				LEFT JOIN device_parameter_filter dpf ON dp.id = dpf.id_device_parameter AND d.id = dpf.id_device AND (dpf.min_value IS NOT NULL OR dpf.max_value IS NOT NULL)
			ORDER BY
				t.time
	</select>
	
	<select id="getDataVirtualDevice" resultType="com.nwm.api.entities.ClientMonthlyDateEntity" >
		SELECT
			time_full,
			categories_time,
			ROUND(nvm_irradiance, 1) AS nvm_irradiance,
			CASE
				WHEN DATEDIFF(#{end_date}, #{start_date}) <![CDATA[ < ]]> 5 THEN ROUND(nvmActivePower, 1)
				ELSE CAST(nvmActiveEnergy AS DECIMAL)
			END AS chart_energy_kwh,
			ROUND(nvmActivePower, 1) AS nvmActivePower,
			CAST(nvmActiveEnergy AS DECIMAL) AS nvmActiveEnergy,
			ROUND(expected_power, 1) AS expected_power,
			ROUND(expected_energy, 1) AS expected_energy
		FROM
			(
						SELECT
							<include refid="com.nwm.common.timeGroupBy">
								<property name="time" value="CONVERT_TZ( dv.time, 'UTC', t.value )" />
								<property name="interval" value="data_send_time" />
							</include> AS time_full,
							<include refid="SitesAnalytics.categoriesTime"><property name="time" value="CONVERT_TZ( dv.time, 'UTC', t.value )" /></include> AS categories_time,
							IF(
								(dpf1.min_value IS NOT NULL AND AVG(dv.nvmActivePower) <![CDATA[ < ]]> dpf1.min_value) OR (dpf1.max_value IS NOT NULL AND AVG(dv.nvmActivePower) <![CDATA[ > ]]> dpf1.max_value),
								NULL,
								AVG(dv.nvmActivePower)
							) AS nvmActivePower,
							IF(
								(dpf2.min_value IS NOT NULL AND SUM(dv.nvmActiveEnergy) <![CDATA[ < ]]> dpf2.min_value) OR (dpf2.max_value IS NOT NULL AND SUM(dv.nvmActiveEnergy) <![CDATA[ > ]]> dpf2.max_value),
								NULL,
								<include refid="com.nwm.common.intervalEnergyCalculationByVirtualDevice">
									<property name="datetime" value="CONVERT_TZ( dv.time, 'UTC', t.value )" />
									<property name="interval" value="data_send_time" />
								</include>
							) AS nvmActiveEnergy,
							AVG(dv.nvm_irradiance) AS nvm_irradiance,
							AVG(dv.expected_power_ac) AS expected_power,
							<include refid="com.nwm.common.calculationByInterval">
								<property name="datetime" value="CONVERT_TZ( dv.time, 'UTC', t.value )" />
								<property name="interval" value="data_send_time" />
							</include> * AVG(dv.expected_power_ac) AS expected_energy
						FROM
							${datatablename} dv
							LEFT JOIN device d ON d.id = dv.id_device
							LEFT JOIN site s ON s.id = d.id_site 
							LEFT JOIN time_zone t ON t.id = s.id_time_zone
							LEFT JOIN device_parameters dp1 ON dp1.id_device_group = d.id_device_group AND dp1.is_active_power = 1 AND ${filterEnabled} = true
							LEFT JOIN device_parameter_filter dpf1 ON dp1.id = dpf1.id_device_parameter AND d.id = dpf1.id_device AND (dpf1.min_value IS NOT NULL OR dpf1.max_value IS NOT NULL)
							LEFT JOIN device_parameters dp2 ON dp2.id_device_group = d.id_device_group AND dp2.is_user_defined = 1 AND dp2.is_energy = 1 AND ${filterEnabled} = true
							LEFT JOIN device_parameter_filter dpf2 ON dp2.id = dpf2.id_device_parameter AND d.id = dpf2.id_device AND (dpf2.min_value IS NOT NULL OR dpf2.max_value IS NOT NULL)
						WHERE
							s.`status` = 1
							AND d.`status` = 1
							AND d.id_site = #{id_site}
							AND d.id_device_type = 12
							AND s.is_delete = 0
							AND d.is_delete = 0
							AND (d.meter_type = 3 OR d.meter_type IS NULL)
							AND (CONVERT_TZ( dv.time, 'UTC', t.value ) BETWEEN #{start_date} AND #{end_date})
							<foreach item="item" index="index" collection="hidden_data_list">
								AND
									CASE
										WHEN d.id = #{item.id_device} THEN CONVERT_TZ( dv.time, 'UTC', t.value ) NOT BETWEEN #{item.date_from} AND #{item.date_to}
										ELSE TRUE
									END
							</foreach>
						GROUP BY
							time_full
						ORDER BY
							time
			) t
	</select>

	<select id="getDataIrradiance" resultType="com.nwm.api.entities.ClientMonthlyDateEntity" >
				SELECT
					<include refid="com.nwm.common.timeGroupBy">
						<property name="time" value="CONVERT_TZ( dv.time, 'UTC', t.value )" />
						<property name="interval" value="data_send_time" />
					</include> AS time_full,
					<include refid="SitesAnalytics.categoriesTime"><property name="time" value="CONVERT_TZ( dv.time, 'UTC', t.value )" /></include> AS categories_time,
					ROUND(AVG(
						<include refid="com.nwm.common.expectedPowerByPvModel">
							<property name="irradiance" value="dv.nvm_irradiance" />
							<property name="temperature" value="dv.nvm_temperature" />
							<property name="panelTemperature" value="dv.nvm_panel_temperature" />
							<property name="power" value="NULL" />
						</include>
					), 1) AS expected_power,
					ROUND(IF(
						AVG(dv.nvm_irradiance) IS NULL,
						NULL,
						AVG(
							<include refid="com.nwm.common.expectedPowerByPvModel">
								<property name="irradiance" value="dv.nvm_irradiance" />
								<property name="temperature" value="dv.nvm_temperature" />
								<property name="panelTemperature" value="dv.nvm_panel_temperature" />
								<property name="power" value="NULL" />
							</include>
						) *
						<include refid="com.nwm.common.calculationByInterval">
							<property name="datetime" value="CONVERT_TZ( dv.time, 'UTC', t.value )" />
							<property name="interval" value="data_send_time" />
						</include>
					), 1) AS expected_energy,
					ROUND(AVG(dv.nvm_irradiance), 1) AS nvm_irradiance
				FROM
					(
						SELECT
							dv.id_device,
							dv.time,
							FROM_UNIXTIME(((UNIX_TIMESTAMP(CONVERT_TZ(dv.time, 'UTC', t.value )) DIV (CASE s.data_send_time WHEN 1 THEN 300 WHEN 2 THEN 900 ELSE 60 END)) * (CASE s.data_send_time WHEN 1 THEN 300 WHEN 2 THEN 900 ELSE 60 END)), '%Y-%m-%d %H:%i:00') AS time_format,
							AVG(dv.nvm_irradiance) AS nvm_irradiance,
							AVG(dv.nvm_temperature) AS nvm_temperature,
							AVG(dv.nvm_panel_temperature) AS nvm_panel_temperature
						FROM
							${datatablename} dv
							JOIN device d ON d.id = dv.id_device
							JOIN site s ON s.id = d.id_site
							JOIN time_zone t ON t.id = s.id_time_zone
						WHERE
							s.id = #{id_site}
							AND dv.time BETWEEN CONVERT_TZ(#{start_date}, t.value, 'UTC') AND CONVERT_TZ(#{end_date}, t.value, 'UTC')
							<foreach item="hidden" index="index" collection="hidden_data_list">
								AND
									CASE
										WHEN d.id = #{hidden.id_device} THEN CONVERT_TZ( dv.time, 'UTC', t.value ) NOT BETWEEN #{hidden.date_from} AND #{hidden.date_to}
										ELSE TRUE
									END
						    </foreach>
							AND s.`status` = 1
							AND d.`status` = 1
							AND d.id = #{id_device}
						GROUP BY
							time_format
					) dv
					JOIN device d ON d.id = dv.id_device
					JOIN site s ON s.id = d.id_site
					JOIN time_zone t ON t.id = s.id_time_zone
				GROUP BY
					time_full
				ORDER BY
					dv.time
	</select>
	
	<select id="getExpectedBySelectedPOA" resultType="com.nwm.api.entities.ClientMonthlyDateEntity" >
		SELECT
			<include refid="com.nwm.common.timeGroupBy">
				<property name="time" value="t.time_format" />
				<property name="interval" value="data_send_time" />
			</include> AS time_full,
			<include refid="SitesAnalytics.categoriesTime"><property name="time" value="t.time_format" /></include> AS categories_time,
			ROUND(AVG(
				<include refid="com.nwm.common.expectedPowerByPvModel">
					<property name="irradiance" value="irradiance" />
					<property name="temperature" value="temperature" />
					<property name="panelTemperature" value="panelTemperature" />
					<property name="power" value="NULL" />
				</include>
			), 1) AS expected_power,
			ROUND(IF(
				AVG(irradiance) IS NULL,
				NULL,
				AVG(
					<include refid="com.nwm.common.expectedPowerByPvModel">
						<property name="irradiance" value="irradiance" />
						<property name="temperature" value="temperature" />
						<property name="panelTemperature" value="panelTemperature" />
						<property name="power" value="NULL" />
					</include>
				) *
				<include refid="com.nwm.common.calculationByInterval">
					<property name="datetime" value="time_format" />
					<property name="interval" value="data_send_time" />
				</include>
			), 1) AS expected_energy
		FROM
			(
				SELECT
					id AS id_site,
					time_format,
					AVG(temperature) AS temperature,
					AVG(panelTemperature) AS panelTemperature,
					AVG(irradiance) AS irradiance
				FROM (
					<foreach collection="panelTemps" item="item" separator="UNION ALL">
						SELECT
							s.id,
							FROM_UNIXTIME(((UNIX_TIMESTAMP(CONVERT_TZ(dv.time, 'UTC', t.value )) DIV (CASE s.data_send_time WHEN 1 THEN 300 WHEN 2 THEN 900 ELSE 60 END)) * (CASE s.data_send_time WHEN 1 THEN 300 WHEN 2 THEN 900 ELSE 60 END)), '%Y-%m-%d %H:%i:00') AS time_format,
							AVG(dv.nvm_temperature) AS temperature,
							AVG(dv.nvm_panel_temperature) AS panelTemperature,
							NULL AS irradiance
						FROM
							${item.datatablename} dv
							JOIN device d ON d.id = dv.id_device
							JOIN site s ON s.id = d.id_site
							JOIN time_zone t ON t.id = s.id_time_zone
						WHERE
							s.id = #{item.id_site}
							AND s.`status` = 1
							AND d.`status` = 1
							AND d.id = #{item.id}
							AND dv.time BETWEEN CONVERT_TZ(#{start_date}, t.value, 'UTC') AND CONVERT_TZ(#{end_date}, t.value, 'UTC')
						GROUP BY
							time_format
					</foreach>
					
					<foreach collection="POAs" item="item" open="UNION ALL" separator="UNION ALL">
						SELECT
							s.id,
							FROM_UNIXTIME(((UNIX_TIMESTAMP(CONVERT_TZ(dv.time, 'UTC', t.value )) DIV (CASE s.data_send_time WHEN 1 THEN 300 WHEN 2 THEN 900 ELSE 60 END)) * (CASE s.data_send_time WHEN 1 THEN 300 WHEN 2 THEN 900 ELSE 60 END)), '%Y-%m-%d %H:%i:00') AS time_format,
							NULL AS temperature,
							NULL AS panelTemperature,
							AVG(dv.nvm_irradiance) AS irradiance
						FROM
							${item.datatablename} dv
							JOIN device d ON d.id = dv.id_device
							JOIN site s ON s.id = d.id_site
							JOIN time_zone t ON t.id = s.id_time_zone
						WHERE
							s.id = #{item.id_site}
							AND s.`status` = 1
							AND d.`status` = 1
							AND d.id = #{item.id}
							AND dv.time BETWEEN CONVERT_TZ(#{start_date}, t.value, 'UTC') AND CONVERT_TZ(#{end_date}, t.value, 'UTC')
						GROUP BY
							time_format
					</foreach>
				) t
				GROUP BY
					time_format
			) t
			JOIN site s ON s.id = t.id_site
		GROUP BY
			time_full
		ORDER BY
			t.time_format
	</select>
	
	<select id="getCustomerViewInfo" resultType="Map" >
		SELECT
			s.id,
			s.kiosk_view,
			SHA1(s.id) AS hash_id,
			<include refid="com.nwm.common.siteName"/> AS `name`,
			s.dc_capacity,
			s.ac_capacity,
			DATE_FORMAT(s.built_since, '%m/%d/%Y %H:%i:%s') AS built_since, 
			DATE_FORMAT(s.commissioning, '%m/%d/%Y %H:%i:%s') AS commissioning, 
			s.gallery,
			<choose> 
		        <when test="domain == 'demo.nextwavemonitoring.com' || domain_role == '1ff1de774005f8da13f42943881c655f'">
					"Hiden for demo"
		        </when>
		        <otherwise>
					s.about
			    </otherwise>                                                  
		    </choose> AS about,
		    s.inv_level,
			s.kiosk_view,
			<choose> 
		        <when test="domain == 'demo.nextwavemonitoring.com' || domain_role == '1ff1de774005f8da13f42943881c655f'">
					"/uploads/logo/demo-logo.jpg"
		        </when>
		        <otherwise>
					s.site_logo
			    </otherwise>                                                  
		    </choose> AS site_logo,
			s.lat,
			s.lng,
			s.data_send_time,
			s.table_data_report,
			s.table_data_virtual,
			IF(v.enable_virtual_device > 0, 1, 0) AS enable_virtual_device,
			t.`value` AS timezone_value,
			ROUND(e.energy_today, 1) AS energy_today,
			ROUND(IFNULL(SUM(IF(<include refid="com.nwm.common.isCurrentValidCondition"/>, d.last_value, NULL)), 0), 2) AS ac_power,
			ROUND(IFNULL(SUM(IF(<include refid="com.nwm.common.isCurrentValidCondition"/>, d.last_value, NULL)), 0), 2) AS peak_power_today,
			IF(e.energy_lifetime > 0, FORMAT(ROUND(e.energy_lifetime * 0.0117, 0), 0) , FORMAT(ROUND(ee.energy_lifetime * 0.0117, 0), 0)) AS total_tree,
			IF(e.energy_lifetime > 0, FORMAT(ROUND((e.energy_lifetime * 392), 0), 0) , FORMAT(ROUND((ee.energy_lifetime * 392), 0), 0)) AS total_co2,
			ROUND(IF(e.energy_lifetime > 0, e.energy_lifetime, ee.energy_lifetime), 0) AS energy_lifetime,
			CASE
			    WHEN s.unit_type_temp = 1 THEN IF(s.weather_indoor_temp_unit = '°C', s.weather_indoor_temp, ROUND(s.weather_indoor_temp * 1.8 + 32,1) )
			    WHEN s.unit_type_temp = 2 THEN IF(s.weather_indoor_temp_unit = '°C', ROUND(s.weather_indoor_temp * 1.8 + 32,1), ROUND((s.weather_indoor_temp) - 32 * 5/9,1))
			    ELSE "-"
			END AS temperature,
			CASE
			    WHEN s.unit_type_temp = 1 THEN "°C"
			    WHEN s.unit_type_temp = 2 THEN "°F"
			    ELSE "N/A"
			END AS temperature_unit,
			s.weather_humidity AS humidity
			
		FROM
			site s
			JOIN time_zone t ON t.id = s.id_time_zone
			<if test="kiosk_view == 0 and is_supper_admin != 1">
				JOIN site_employee_map scm ON scm.id_site = s.id AND scm.id_employee= #{id_employee}
			 </if>
			LEFT JOIN device d ON (
				d.id_site = s.id
				AND d.`status` = 1
				AND d.is_delete = 0
				AND IF(#{totalMeter} > 0, d.id_device_type IN (3,7,9) AND d.is_excluded_meter = 0, d.id_device_type = 1)
			)
			LEFT JOIN (
				SELECT
					id,
					SUM(energy_today) AS energy_today,
					SUM(energy_lifetime) AS energy_lifetime
				FROM (
					<foreach collection="groupMeter" item="item" index="index" separator="union all">
						SELECT
							s.id,
							nvmActiveEnergy - (SELECT nvmActiveEnergy FROM ${item.datatablename} WHERE time = m.todayMinTime AND id_device = #{item.id}) AS energy_today,
							nvmActiveEnergy - (SELECT nvmActiveEnergy FROM ${item.datatablename} WHERE time = m.lifetimeMinTime AND id_device = #{item.id}) AS energy_lifetime
						FROM
							${item.datatablename} dv
							JOIN device d ON d.id = dv.id_device
							JOIN site s ON s.id = d.id_site 
							JOIN (
								SELECT
									(
										SELECT
											MIN(time)
										FROM
											${item.datatablename}
										WHERE
											id_device = #{item.id}
											AND time >= CONVERT_TZ(DATE_FORMAT(CONVERT_TZ(UTC_TIMESTAMP(), 'UTC', #{item.timezone_value}), '%Y-%m-%d 00:00:00'), #{item.timezone_value}, 'UTC')
											AND error = 0
									) AS todayMinTime,
									MIN(time) AS lifetimeMinTime,
									MAX(time) AS maxTime
								FROM
									${item.datatablename}
								WHERE
									id_device = #{item.id}
									AND error = 0
							) m ON m.maxTime = dv.time AND dv.id_device = #{item.id}
						WHERE
							s.id = #{item.id_site}
							AND s.`status` = 1 
							AND d.id = #{item.id}
							AND d.`status` = 1
					</foreach>
				) t 
			) e ON e.id = s.id
			LEFT JOIN (
				SELECT
					COUNT(*) AS enable_virtual_device,
					d.id_site
				FROM
					device d 
				WHERE
					d.`status` = 1 
					AND d.is_delete = 0 
					AND d.hidden = 0
					AND d.virtual_device_type IS NOT NULL
					AND d.virtual_device_type != ''
					AND IF(#{hash_id} IS NOT NULL, SHA1(d.id_site) = #{hash_id}, d.id_site = #{id_site})
			) v ON v.id_site = s.id
			LEFT JOIN(
				SELECT
					d.id_site,
					ROUND(SUM(d.energy_today), 1) AS energy_today,
					ROUND(SUM(d.last_value), 2) AS peak_power_today,
					ROUND(SUM(d.energy_lifetime), 0) AS energy_lifetime
				FROM
					device d
					LEFT JOIN site s ON s.id = d.id_site
				WHERE 
					IF(#{hash_id} IS NOT NULL, SHA1(s.id) = #{hash_id}, s.id = #{id_site})
					AND s.`status` = 1 
					AND s.is_delete = 0
					AND d.`status` = 1
					<choose>
						<when test="totalMeter > 0">
							AND d.id_device_type IN (3,7,9)
							AND d.is_excluded_meter = 0
						</when>
						<otherwise>
							AND d.id_device_type IN (1)
						</otherwise>
					</choose>
				GROUP BY d.id_site
			) ee ON ee.id_site = s.id
		WHERE
			IF(#{hash_id} IS NOT NULL, SHA1(s.id) = #{hash_id}, s.id = #{id_site})
			AND s.`status` = 1 
			AND s.is_delete = 0
			<if test="kiosk_view == 1">AND s.kiosk_view = 1</if>
			AND (CONVERT_TZ(UTC_TIMESTAMP(), 'UTC', t.value) <![CDATA[ < ]]> s.expiration OR s.expiration IS NULL)
		GROUP BY
			s.id;
		
	</select>
	
	<select id="getList" resultType="Map">
		SELECT
			s.id,
			SHA1(s.id)  hash_id,
			s.id_country,
			s.id_time_zone,
			<include refid="com.nwm.common.siteName"/> AS `name`,
			s.street,
			s.lat,
			s.lng,
			built_since,
			s.old_data,
			s.number,
			s.postal_code,
			s.city,
			s.state,
			DATE_FORMAT(s.commissioning, '%m/%d/%Y') AS commissioning,
			s.emergency_contact,
			s.ac_capacity,
			s.dc_capacity,
			s.status,
			s.is_delete,
			s.created_date,
			s.created_by,
			s.updated_date,
			s.updated_by,
			s.gallery,
			s.site_default,
			s.data_send_time,
			s.table_data_report,
			s.table_data_virtual,
			s.display_time_format,
			d.java_format AS java_date_format,
			TIME_FORMAT(CONVERT_TZ(s.sunrise, 'UTC', tz.`value`), '%H:%i:%s') AS sunrise,
			TIME_FORMAT(CONVERT_TZ(s.sunset, 'UTC', tz.`value`), '%H:%i:%s') AS sunset,
			tz.`value` AS timezone_value,
			IF(v.enable_virtual_device > 0 , 1, 0) AS enable_virtual_device,
			CONCAT_WS(', ',s.street, s.city, s.state, c.`name`, s.postal_code) AS address_full
		FROM
			site AS s
			LEFT JOIN country c ON c.id = s.id_country
			LEFT JOIN site_employee_map scm ON scm.id_site = s.id
			LEFT JOIN time_zone tz ON tz.id = s.id_time_zone
			LEFT JOIN date_format d ON d.id = s.display_date_format
			LEFT JOIN (
				SELECT
					COUNT(*) AS enable_virtual_device,
					d.id_site
				FROM
					device d 
				WHERE
					d.`status` = 1 
					AND d.is_delete = 0 
					AND d.hidden = 0
					AND d.virtual_device_type IS NOT NULL
					AND d.virtual_device_type != ''
				GROUP BY
					d.id_site
			) v ON v.id_site = s.id
		WHERE
			 s.is_delete = 0
			 AND s.status = 1 
			 <if test="is_supper_admin != 1">
				 AND scm.id_employee= #{id_employee}
			 </if>
		GROUP BY
			s.id
		ORDER BY
			s.`name` ASC
	</select>
	
	
	<select id="getAlertSummary" resultType="com.nwm.api.entities.AlertEntity" parameterType="com.nwm.api.entities.AlertEntity">
		SELECT (
			SELECT
				COUNT( e.id_error_level ) AS lowPriority 
			FROM
				alert a
				LEFT JOIN error e ON a.id_error = e.id
				LEFT JOIN device d ON d.id = a.id_device
				LEFT JOIN site s ON s.id = d.id_site
				LEFT JOIN site_employee_map scm ON s.id = scm.id_site
			WHERE
				e.id_error_level = 13
				AND a.`status` = 1 
				AND e.`status` = 1
				AND s.status = 1
				AND s.is_delete = 0
				AND scm.id_employee = #{id_customer}
				AND scm.is_hiding = 0
			GROUP BY
				e.id_error_level
		) AS lowPriority,
		
		(
			SELECT
				COUNT( e.id_error_level ) AS highPriority 
			FROM
				alert a
				LEFT JOIN error e ON a.id_error = e.id 
				LEFT JOIN device d ON d.id = a.id_device
				LEFT JOIN site s ON s.id = d.id_site
				LEFT JOIN site_employee_map scm ON s.id = scm.id_site
			WHERE
				e.id_error_level = 11
				AND a.`status` = 1 
				AND e.`status` = 1 
				AND s.status = 1
				AND s.is_delete = 0
				AND scm.id_employee = #{id_customer}
				AND scm.is_hiding = 0
			GROUP BY
				e.id_error_level
		) AS highPriority,
		
		
		(
			SELECT
				COUNT(*) AS totalError 
			FROM
				alert a
				LEFT JOIN error e ON a.id_error = e.id 
				LEFT JOIN device d ON d.id = a.id_device
				LEFT JOIN site s ON s.id = d.id_site
				LEFT JOIN site_employee_map scm ON s.id = scm.id_site
			WHERE
				a.`status` = 1 
				AND e.`status` = 1 
				AND s.status = 1
				AND s.is_delete = 0
				AND scm.id_employee = #{id_customer}
				AND scm.is_hiding = 0
				<if test="!isUserNW.booleanValue()">
					AND e.id_error_level != 14
				</if>
		) AS totalError
		
				
  	</select>
  	
  	
  	<select id="getListAlertCustomerView" resultType="Map" parameterType="String" >
		SELECT
			l.id,
			l.asset,
			el.name AS priority_name,
			l.capacity,
			l.`status`,
			l.alert_acknowledged,
			l.disable_notification,
			l.resolved,
			IF(l.`status` = 1, 'Open', 'Close') AS status_name,
			el.`name` AS `level`,
			e.message,
			e.error_code,
			d.devicename,
			<include refid="com.nwm.common.siteName"/> AS site_name,
			s.id AS id_site,
			d.id as id_device,
			i.icon AS icon_alert,
			el.id AS id_error_level,
			dt.name AS device_type_name,
			el.color AS color_error_level,
			el.level,
			DATE_FORMAT( CONVERT_TZ( l.start_date, t.`offset`, #{offset_timezone} ), #{format_sql_long} ) AS start,
			DATE_FORMAT( CONVERT_TZ( l.start_date,'+00:00', t.`offset`) , '%m/%d/%Y %h:%i %p') AS start_date,
			DATE_FORMAT(CONVERT_TZ( l.end_date,'+00:00', t.`offset`), '%m/%d/%Y %h:%i %p') AS end_date,
			
			CONCAT_WS('', 
				IF( FLOOR((TIMESTAMPDIFF(MINUTE,CONVERT_TZ( l.start_date,'+00:00', t.`offset`),#{current_time}) / (60*24))) <![CDATA[<]]> 0 , 0, FLOOR((TIMESTAMPDIFF(MINUTE,CONVERT_TZ( l.start_date,'+00:00', t.`offset`), #{current_time}) / (60*24)))), 'd ',
				IF(MOD(FLOOR((TIMESTAMPDIFF(MINUTE,CONVERT_TZ( l.start_date,'+00:00', t.`offset`),#{current_time}) / 60)), 24) <![CDATA[<]]> 0, 0, MOD(FLOOR((TIMESTAMPDIFF(MINUTE,CONVERT_TZ( l.start_date,'+00:00', t.`offset`),#{current_time}) / 60)), 24)), 'h ',
				IF( MOD((TIMESTAMPDIFF(MINUTE,CONVERT_TZ( l.start_date,'+00:00', t.`offset`), #{current_time})), 60) <![CDATA[<]]> 0, 0, MOD((TIMESTAMPDIFF(MINUTE,CONVERT_TZ( l.start_date,'+00:00', t.`offset`), #{current_time})), 60)), 'm'
			) AS duration,
			IFNULL( FLOOR((TIMESTAMPDIFF(MINUTE,CONVERT_TZ( l.start_date,'+00:00', t.`offset`), #{current_time}))) , 0) AS open_period,
			
			CASE
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL -1 DAY ) THEN TIMESTAMPDIFF( DAY, CONVERT_TZ( l.start_date, '+00:00', t.`offset` ), DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i') )
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL -1 HOUR ) THEN TIMESTAMPDIFF( HOUR, CONVERT_TZ( l.start_date, '+00:00', t.`offset` ), DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i') ) 
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL -1 MINUTE ) THEN TIMESTAMPDIFF( MINUTE, CONVERT_TZ( l.start_date, '+00:00', t.`offset` ), DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i') ) 
				ELSE 0
			END AS times_ago,

	
			CASE
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL - 1 DAY ) THEN 'day'
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL - 1 HOUR ) THEN 'hour'
				WHEN CONVERT_TZ( l.start_date, '+00:00', t.`offset` ) <![CDATA[<]]> DATE_ADD( DATE_FORMAT( CONVERT_TZ( NOW(), '+00:00', t.`offset` ) , '%Y-%m-%d %H:%i'), INTERVAL - 1 MINUTE ) THEN 'minute' ELSE 'now'
			END AS times_ago_unit,
			<include refid="com.nwm.common.keyIndicatorField"/>,
			<include refid="com.nwm.common.unitField"/>
			
		FROM
			`alert` l
			LEFT JOIN error e ON e.id = l.id_error
			LEFT JOIN error_level el ON el.id = e.id_error_level
			LEFT JOIN icon i ON i.id = el.id_icon
			LEFT JOIN device d ON d.id = l.id_device
			LEFT JOIN device_group dg ON dg.id = d.id_device_group
			LEFT JOIN site s ON s.id = d.id_site
			LEFT JOIN site_employee_map scm ON s.id = scm.id_site
			LEFT JOIN time_zone t ON t.id = s.id_time_zone
			LEFT JOIN device_type dt ON dt.id = d.id_device_type
			WHERE e.`status` = 1 AND e.is_delete = 0
			AND l.is_delete = 0
			AND s.status = 1
			AND s.is_delete = 0
			AND scm.id_employee = #{id_customer}
			AND scm.is_hiding = 0
			<if test="view_history == 'active'">
				AND l.status = 1
			</if>
			<if test="view_history != 'active'">
				AND l.status = 0
			</if>
			
			<if test="date_from != null and date_to != null">
				AND CONVERT_TZ( l.start_date, '+00:00', t.`offset` )  BETWEEN #{date_from}  AND #{date_to}
			</if>
			<if test="id_sites != null and !id_sites.isEmpty()">
				AND s.id IN  (
					<foreach item="item" index="index" collection="id_sites" separator=" , ">
						#{item.id}
					</foreach>
				)
			</if>
			
			<if test="id_levels != null and !id_levels.isEmpty()">
				AND el.id IN  (
					<foreach item="item" index="index" collection="id_levels" separator=" , ">
						#{item.id}
					</foreach>
				)
			</if>
			
			<if test="site_name != null">
				AND s.`name` LIKE CONCAT("%",#{site_name}, "%")
			</if>
			
			<if test="!isUserNW.booleanValue()">
				AND el.id != 14
			</if>
			
			GROUP BY l.id 
			
			<choose>
				<when test="sort_column == 'id'">
			      ORDER BY l.`id` ${order_by}
			    </when>
			    
			    <when test="sort_column == 'site_name'">
			      ORDER BY s.`name` ${order_by}
			    </when>
			    
			    <when test="sort_column == 'priority_name'">
			      ORDER BY el.`name` ${order_by}
			    </when>
			    
			    <when test="sort_column == 'message'">
			      ORDER BY e.`message` ${order_by}
			    </when>
			    
			    <when test="sort_column == 'devicename'">
			      ORDER BY d.`devicename` ${order_by}
			    </when>
			    
			    <when test="sort_column == 'times_ago'">
			      ORDER BY l.start_date ${order_by}
			    </when>
			    
			    <when test="sort_column == 'start_date'">
			      ORDER BY l.`start_date` ${order_by}
			    </when>
			    
			    <when test="sort_column == 'end_date'">
			      ORDER BY l.`end_date` ${order_by}
			    </when>
			    
			    <when test="sort_column == 'open_period'">
			      ORDER BY `open_period` ${order_by}
			    </when>
			    
			    <when test="sort_column == 'device_type_name'">
			      ORDER BY dt.name ${order_by}
			    </when>
			    
			    <when test="sort_column == 'key_indicator'">
			      ORDER BY unit, d.field_value1 ${order_by}
			    </when>
			    
			    <otherwise>
			      ORDER BY id DESC
			    </otherwise>
			  </choose>
  
			LIMIT #{limit} OFFSET #{offset};
	</select>
	
	
	<select id="countAlertCustomerView"  resultType="int" parameterType="String">
    	SELECT
			COUNT(l.id) as totalRecord
			
		FROM
			`alert` l
			LEFT JOIN error e ON e.id = l.id_error
			LEFT JOIN error_level el ON el.id = e.id_error_level
			LEFT JOIN device d ON d.id = l.id_device
			LEFT JOIN site s ON s.id = d.id_site
			LEFT JOIN site_employee_map scm ON s.id = scm.id_site
			LEFT JOIN time_zone t ON t.id = s.id_time_zone
			WHERE  e.`status` = 1 AND e.is_delete = 0 AND scm.id_employee = #{id_customer} AND s.status = 1 AND s.is_delete = 0
			
			<if test="view_history == 'active'">
				AND l.status = 1
			</if>
			<if test="view_history != 'active'">
				AND l.status = 0
			</if>
			
			<if test="date_from != null and date_to != null">
				AND CONVERT_TZ( l.start_date, '+00:00', t.`offset` )  BETWEEN #{date_from}  AND #{date_to}
			</if>

			<if test="id_sites != null and !id_sites.isEmpty()">
				AND s.id IN  (
					<foreach item="item" index="index" collection="id_sites" separator=" , ">
						#{item.id}
					</foreach>
				)
			</if>
			
			<if test="id_levels != null and !id_levels.isEmpty()">
				AND el.id IN  (
					<foreach item="item" index="index" collection="id_levels" separator=" , ">
						#{item.id}
					</foreach>
				)
			</if>
			
			<if test="site_name != null">
				AND s.`name` LIKE CONCAT("%",#{site_name}, "%")
			</if>
			
			<if test="!isUserNW.booleanValue()">
				AND el.id != 14
			</if>
  	</select>
  	
	

  	
  	<select id="countCustomerViewNotificationAlert" resultType="int" parameterType="com.nwm.api.entities.CustomerEntity">
	  	SELECT
			COUNT( DISTINCT a.id ) AS totalRecord 
		FROM
			site s
			LEFT JOIN site_employee_map scm ON s.id = scm.id_site
			LEFT JOIN device d ON d.id_site = s.id
			LEFT JOIN alert a ON a.id_device = d.id 
			LEFT JOIN error e ON e.id = a.id_error
		WHERE
			
			s.`status` = 1 
			AND s.is_delete = 0 
			AND a.`status` = 1 
			AND a.is_delete = 0 
			AND d.`status` = 1 
			AND d.is_delete = 0
			AND scm.id_employee = #{id_customer}
			AND scm.is_hiding = 0
			<if test="id_sites != null and !id_sites.isEmpty()">
			AND s.id IN  (
				<foreach item="item" index="index" collection="id_sites" separator=", ">
					#{item.id}
				</foreach>
			)
			</if>
			<if test="!isUserNW.booleanValue()">
				AND e.id_error_level != 14
			</if>
			
  	</select>
  	
	<select id="getSitePowerChart" resultType="com.nwm.api.entities.ClientMonthlyDateEntity" >
		SELECT
			t.time_full,
			t.categories_time,
			CAST(SUM(t.nvmActivePower) AS DECIMAL) AS nvmActivePower
		FROM
			(
				<foreach collection="devices" item="item" separator="UNION ALL">
					SELECT
						FROM_UNIXTIME(((UNIX_TIMESTAMP(CONVERT_TZ(dv.time, 'UTC', #{item.timezone_value})) DIV 900)*900), '%Y-%m-%d %H:%i') AS time_full,
						FROM_UNIXTIME(((UNIX_TIMESTAMP(CONVERT_TZ(dv.time, 'UTC', #{item.timezone_value})) DIV 900)*900), '%d. %b %H:%i') AS categories_time,
						AVG(dv.nvmActivePower) AS nvmActivePower
					FROM
						${item.datatablename} dv
					WHERE
						dv.time BETWEEN CONVERT_TZ(#{start_date}, #{item.timezone_value}, 'UTC') AND CONVERT_TZ(#{end_date}, #{item.timezone_value}, 'UTC')
					GROUP BY
						time_full
				</foreach>
			) t 
		GROUP BY
			t.time_full
	</select>
  	
	
</mapper>